{
  "name": "coincheck-client",
  "description": "Coincheck API wrapper for Node.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "@commitlint/config-lerna-scopes": "^11.0.0",
    "commitizen": "^4.2.2",
    "husky": "^5.0.4",
    "lerna": "^3.22.1",
    "pinst": "^2.1.1",
    "prettier": "^2.2.1"
  },
  "workspaces": [
    "packages/**"
  ],
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
